apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-mailsender-pvc
  labels:
    app: redis-mailsender
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-mailsender
  labels:
    app: redis-mailsender
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-mailsender
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: redis-mailsender
    spec:
      containers:
        - name: redis
          image: redis:alpine
          ports:
            - containerPort: 6379
              protocol: TCP
          
          command:
            - redis-server
            - --requirepass
            - "R3d!sM@ilS3nd3r2025"
            - --appendonly
            - "yes"
          
          env:
            - name: TZ
              value: "America/Manaus"
          
          volumeMounts:
            - name: redis-data
              mountPath: /data
          
          livenessProbe:
            exec:
              command:
                - redis-cli
                - --raw
                - incr
                - ping
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          
          readinessProbe:
            exec:
              command:
                - redis-cli
                - --raw
                - incr
                - ping
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
      
      restartPolicy: Always
      
      volumes:
        - name: redis-data
          persistentVolumeClaim:
            claimName: redis-mailsender-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: redis-mailsender
  labels:
    app: redis-mailsender
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    app: redis-mailsender
